{
  "dependencies": {
    "bulma": "^1.0.4",
    "leaflet": "^1.9.4",
    "leaflet.fullscreen": "^4.0.0",
    "protomaps-leaflet": "^5.1.0"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.21",
    "@types/leaflet.fullscreen": "^3.0.3",
    "typescript": "^5.9.3",
    "vite": "^7.2.2"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "wasm": "wasm-pack build ./rust-wasm --target web --out-dir ../pkg",
    "test:rust": "cargo test --manifest-path rust-wasm/Cargo.toml",
    "test:ts": "vitest run",
    "typecheck": "tsc --noEmit"
  }
}
